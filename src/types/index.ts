// TypeScript types and interfaces
export interface User {
  id: string;
  email: string;
  createdAt: Date;
}

export interface Record {
  id: string;
  title: string;
  content: string;
  createdAt: Date;
}

// More types will be added in later plans
